interface NoteSaveStatusProps {
  isPending: boolean;
  isDirty: boolean;
  isError: boolean;
}

export default function NoteSaveStatus({
  isPending,
  isDirty,
  isError,
}: NoteSaveStatusProps) {
  return <div></div>;
}
